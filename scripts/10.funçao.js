// FUNÇÃO
// somar
function somar(a, b){
    return a + b
}

const somar = somar(2, 3)

console.log (`somar: ${somar}`)
//subtraçao
function sub(a, b)
    return a - b
}
const sub = somar(2, 3)
console.log (`Sub: ${sub}`)
//mutiplicaçao
function mut(a, b){
    return a * b
}
const mut = somar(2, 3)
console.log (`Soma: ${soma}`)

//divisao
function somar(a, b){
    return a + b
}
const soma = somar(2, 3)
console.log (`Soma: ${soma}`)
